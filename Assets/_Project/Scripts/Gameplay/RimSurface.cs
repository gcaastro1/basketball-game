using UnityEngine;

namespace Basket.Gameplay
{
    // Marks the rim colliders. The ball touching the rim resets the shot clock and makes
    // a last free throw live.
    public class RimSurface : MonoBehaviour { }
}
